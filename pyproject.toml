[project]
name = "pipelines"
version = "0.1.0"
description = "Automações"
readme = "README.md"
authors = [
    { name = "Herian", email = "herian.cavalcante@live.com" }
]
requires-python = ">=3.13"
dependencies = [
    "pandas>=3.0.2",
    "prefect>=3.6.21",
    "requests>=2.32.5",
]
[tool.uv]
package = false

[dependency-groups]
dev = [
    "isort>=8.0.1",
    "ruff>=0.15.5",
]
