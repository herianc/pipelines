[project]
name = "pipelines"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "discord-py>=2.7.1",
    "prefect>=3.6.21",
    "requests>=2.32.5",
]

[dependency-groups]
dev = [
    "isort>=8.0.1",
    "ruff>=0.15.5",
]
